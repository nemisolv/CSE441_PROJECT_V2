<!-- res/drawable/rounded_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@drawable/background" /><!-- Background color -->
    <gradient android:startColor="@color/purple_linear_bottom" android:endColor="@color/purple_linear_top"/>
    <corners android:radius="12dp" /> <!-- Corner radius -->
    <stroke
        android:width="1dp"
        android:color="@color/gray" /> <!-- Optional border color and width -->
</shape>
